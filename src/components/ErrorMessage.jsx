import React from "react";

const ErrorMessage = () => {
  return (
    <h3 className='text-center'>
      That pokemon does not exist or you did not spell the pokemon name right.
    </h3>
  );
};

export default ErrorMessage;
